using System.Collections;
using System.Collections.Generic;
using System.Collections.Specialized;
using UnityEngine;

public class SettingTangibleGroup : SettingTangible
{
    // Moegliche Verbindungen fuer SettingTangibleGruppe
    /*
    public void gruppenVerbindung()
    {
        if (noch leer)
        {
            CreateConnection(mit allem);
        }
        else if (helligkeit schon da)
        {
            CreateConnection(nur mit Farbe);
        }
        else if (farbe schon vorhanden)
        {
            CreateConnection(nur mit Helligkeit verbinden);
        }
        else if (an und aus)
        {
            CreateConnection();
        }
        else if (rolladen)
        {
            CreateConnection();
        }
        else if (temperatur)
        {
            CreateConnection();
        }
        else if (zeit)
        {
            CreateConnection();
        }
    }
    */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
